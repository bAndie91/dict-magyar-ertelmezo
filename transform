#!/bin/bash

set -euo pipefail

find szavak/ -name '*.html' -printf '%p\t%f\n' |\
while IFS=$'\t' read -r path szo
do
	szo=${szo%.*}
	echo "$szo"
	
	./transform-article "$path" | sed -e 's/^/\t/'
done
